services:
  postgres:
    image: postgres:16-alpine
    container_name: hunter_postgres
    restart: always
    environment:
      POSTGRES_DB: hunter_nexus_db
      POSTGRES_PASSWORD: hunter_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

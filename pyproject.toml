[project]
name = "hunter-nexus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "juliogo666-art", email = "julio.go666@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "psycopg2-binary>=2.9.12",
    "psycopg[binary]>=3.3.5",
    "pydantic>=2.13.5",
]

[project.scripts]
hunter-nexus = "hunter_nexus:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
